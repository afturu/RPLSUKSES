import React, { useState, useRef } from 'react';
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  KeyboardAvoidingView,
  ScrollView,
} from 'react-native';
import { Feather } from '@expo/vector-icons';

const FormorderjastipWidget = () => {
  const [text1, setText1] = useState('');
  const [text2, setText2] = useState('');
  const [text3, setText3] = useState('');

  const textInput1 = useRef();
  const textInput2 = useRef();
  const textInput3 = useRef();

  return (
    <KeyboardAvoidingView style={styles.container}>
      <ScrollView showsVerticalScrollIndicator={false}>
        <View style={styles.headerContainer}>
          <Text style={styles.headerText}>Jasa Titip</Text>
        </View>
        <View style={styles.inputContainer1}>
          <TextInput
            style={styles.textInput}
            value={text1}
            onChangeText={setText1}
            ref={textInput1}
            placeholder="Alamat Tujuan"
            underlineColorAndroid="transparent"
            autoCapitalize="none"
          />
        </View>
        <View style={styles.inputContainer2}>
          <TextInput
            style={styles.textInput}
            value={text2}
            onChangeText={setText2}
            ref={textInput2}
            placeholder="Tempat Order"
            underlineColorAndroid="transparent"
            autoCapitalize="none"
          />
        </View>
        <View style={styles.inputContainer3}>
          <TextInput
            style={styles.textArea}
            value={text3}
            onChangeText={setText3}
            ref={textInput3}
            placeholder="Detail pesanan"
            underlineColorAndroid="transparent"
            autoCapitalize="none"
            multiline
            numberOfLines={6}
          />
        </View>
        <TouchableOpacity style={styles.button} onPress={() => {}}>
          <View style={styles.buttonContent}>
            <Text style={styles.buttonText}>Submit</Text>
            <Feather name="check" size={15} color="white" />
          </View>
        </TouchableOpacity>
      </ScrollView>
    </KeyboardAvoidingView>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#FFFFFF',
  },
  headerContainer: {
    padding: 16,
  },
  headerText: {
    fontSize: 24,
    fontWeight: 'bold',
  },
  inputContainer1: {
    padding: 16,
  },
  inputContainer2: {
    padding: 16,
  },
  inputContainer3: {
    padding: 16,
  },
  textInput: {
    borderWidth: 2,
    borderColor: '#E5E7EB',
    borderRadius: 12,
    padding: 12,
    fontSize: 14,
    fontWeight: '500',
  },
  textArea: {
    borderWidth: 2,
    borderColor: '#E5E7EB',
    borderRadius: 12,
    padding: 12,
    fontSize: 14,
    fontWeight: '500',
    height: 160,
    textAlignVertical: 'top',
  },
  button: {
    backgroundColor: '#6F61EF',
    borderRadius: 60,
    padding: 12,
    alignItems: 'center',
    justifyContent: 'center',
    margin: 16,
  },
  buttonContent: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  buttonText: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: '500',
    marginRight:8,
  },
});

export default FormorderjastipWidget;